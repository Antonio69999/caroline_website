{% block body %}
    <div>
        <a href="{{ path('app_home')}}">
            <img width="50px" src="{{ asset('asset/images/logo.png') }}"></img>
    </a>
</div>
<video id="video" width="100%" height="100%" controls loop>
    <source src="{{ asset('asset/videos/video.mp4') }}" type="video/mp4">
    Your browser does not support the video tag.
</video>{% endblock %}{% block footer %}{% endblock %}
