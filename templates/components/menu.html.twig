<nav class="menu">
    <ul>
        <div class="logo"></div>
        <li>
            <h1>Caroline MARCELLIER</h1>
        </li>
        {% for item in categories %}
            <li>
                <a href={{ path('app_category_show', {'id': item.id} ) }}>{{ item.titre }}</a>
            </li>
        {% endfor %}
    </ul>
</nav>
